let Koa;_df9‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);require('dotenv').config();



const { PPRT, MONGO_URI } = process.env;

const app = new Koa();

app.use((ctx) => {
  ctx.body = 'hello';
});

const port = PORT || 4000;
app.listen(port, () => {
  _df9‍.g.console.log('Listenning to port %d', port);
});
